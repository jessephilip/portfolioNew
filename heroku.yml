setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
  config:
    DATABASE_URL: DATABASE_URL

build:
  docker:
    web: Dockerfile
  config:
    DATABASE_URL: DATABASE_URL

release:
  image: web
  config:
    DATABASE_URL: DATABASE_URL
